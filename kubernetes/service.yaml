apiVersion: v1
kind: Service
metadata:
  name: auramarket-web-service
  labels:
    app: auramarket-web
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: auramarket-web
