apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-eks-pvc
  namespace: auru
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 5Gi
