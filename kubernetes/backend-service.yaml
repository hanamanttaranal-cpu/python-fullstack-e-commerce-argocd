apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: auru
spec:
  selector:
    app: backend
  type: NodePort
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
      nodePort: 30082
